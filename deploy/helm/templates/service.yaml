apiVersion: v1
kind: Service
metadata:
  name: {{ include "huddle.fullname" . }}
  labels: {{- include "huddle.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "huddle.selectorLabels" . | nindent 4 }}
