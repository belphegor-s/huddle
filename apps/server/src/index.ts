/**
 * Node entry point for self hosted installs. It will mount the same
 * packages/api app that the Worker mounts, wired to the Node adapter.
 * Built next, together with packages/adapter-node.
 */
export {};
